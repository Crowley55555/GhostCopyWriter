# 🚀 Руководство по развертыванию Ghostwriter

Полное руководство по развертыванию микросервисной архитектуры с соблюдением требований 152-ФЗ о персональных данных.

## 🏗️ Архитектура развертывания

### 🇷🇺 Российский сервер (Django)
- **Назначение**: Хранение и обработка персональных данных пользователей
- **Компоненты**: Django + PostgreSQL + Redis + Nginx
- **Данные**: Профили пользователей, история генераций, шаблоны настроек
- **Соответствие**: Полное соблюдение 152-ФЗ о персональных данных

### 🌐 Зарубежный сервер (Flask AI)
- **Назначение**: AI обработка обезличенных данных
- **Компоненты**: Flask + OpenAI API + Redis + Nginx
- **Данные**: Только обезличенные промпты и результаты генерации
- **Преимущества**: Доступ к OpenAI API, масштабируемость AI обработки

### 🔐 Безопасная связь
- **Шифрование**: Fernet encryption для всех данных между сервисами
- **Протокол**: HTTPS с SSL сертификатами
- **Изоляция**: Нет передачи персональных данных в AI сервис

## 📋 Предварительные требования

### Системные требования

#### Российский сервер (Django)
- **ОС**: Ubuntu 20.04+ / CentOS 8+ / Debian 11+
- **RAM**: Минимум 2GB, рекомендуется 4GB
- **CPU**: 2+ ядра
- **Диск**: 20GB+ SSD
- **Сеть**: Стабильное подключение к интернету

#### Зарубежный сервер (Flask)
- **ОС**: Ubuntu 20.04+ / CentOS 8+ / Debian 11+
- **RAM**: Минимум 1GB, рекомендуется 2GB
- **CPU**: 1+ ядра
- **Диск**: 10GB+ SSD
- **Сеть**: Высокоскоростное подключение (для OpenAI API)
- **Расположение**: Страны с доступом к OpenAI API

### Программное обеспечение
- **Docker**: 20.10+
- **Docker Compose**: 2.0+
- **Git**: Для клонирования репозитория
- **SSL сертификаты**: Let's Encrypt или коммерческие

## 🚀 Пошаговое развертывание

### Этап 1: Подготовка серверов

#### На российском сервере:
```bash
# Обновление системы
sudo apt update && sudo apt upgrade -y

# Установка Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh
sudo usermod -aG docker $USER

# Установка Docker Compose
sudo curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
sudo chmod +x /usr/local/bin/docker-compose

# Клонирование проекта
git clone https://github.com/yourusername/Ghostwriter.git
cd Ghostwriter
```

#### На зарубежном сервере:
```bash
# Те же команды установки Docker
# ...

# Клонирование только Flask части
git clone https://github.com/yourusername/Ghostwriter.git
cd Ghostwriter/flask_generator
```

### Этап 2: Настройка конфигурации

#### Российский сервер (.env):
```env
# Django настройки
DJANGO_SECRET_KEY=your-super-secret-key-here
ALLOWED_HOSTS=yourdomain.ru,www.yourdomain.ru

# База данных
DB_PASSWORD=secure-db-password

# GigaChat API (российский AI)
GIGACHAT_CLIENT_ID=your-gigachat-client-id
GIGACHAT_CLIENT_SECRET=your-gigachat-client-secret

# URL зарубежного Flask сервера
FLASK_EXTERNAL_URL=https://ai.yourcompany.com

# Шифрование (НЕ ИЗМЕНЯЙТЕ)
GENERATOR_ENCRYPTION_KEY=k6W1hS1TpK-fOe-1pEGSSXmSDHkQNrpsI-TfuL-7EHI=
```

#### Зарубежный сервер (flask_generator/.env):
```env
# OpenAI API
OPENAI_API_KEY=your-openai-api-key

# Шифрование (ДОЛЖЕН СОВПАДАТЬ с Django)
GENERATOR_ENCRYPTION_KEY=k6W1hS1TpK-fOe-1pEGSSXmSDHkQNrpsI-TfuL-7EHI=

# Настройки производительности
OPENAI_MODEL=gpt-3.5-turbo
DALLE_MODEL=dall-e-3
REQUEST_TIMEOUT=300
```

### Этап 3: Развертывание сервисов

#### Российский сервер (Django):
```bash
# Развертывание через скрипт
chmod +x deploy/deploy-django.sh
./deploy/deploy-django.sh

# Или вручную
docker-compose -f docker-compose.production.yml up -d
```

#### Зарубежный сервер (Flask):
```bash
# Развертывание через скрипт
chmod +x deploy/deploy-flask.sh
./deploy/deploy-flask.sh

# Или вручную
docker-compose -f docker-compose.flask.yml up -d
```

### Этап 4: Проверка работоспособности

#### Проверка Django сервера:
```bash
# Health check
curl -f http://your-django-domain.ru/

# Админ панель
curl -f http://your-django-domain.ru/admin/

# API статус
docker-compose logs django
```

#### Проверка Flask сервера:
```bash
# Health check
curl -f http://your-flask-domain.com/

# Тестовый endpoint
curl -X POST -H "Content-Type: application/json" \
  -d '{"test": "connection"}' \
  http://your-flask-domain.com/test

# Логи
docker-compose -f docker-compose.flask.yml logs flask-ai
```

#### Проверка связи между сервисами:
```bash
# На Django сервере проверьте доступность Flask
docker-compose exec django python manage.py shell -c "
from generator.views import check_flask_api_status
print('Flask API доступен:', check_flask_api_status())
"
```

## 🔒 Настройка безопасности

### SSL сертификаты

#### Получение Let's Encrypt сертификатов:
```bash
# Установка certbot
sudo apt install certbot python3-certbot-nginx

# Получение сертификата для Django
sudo certbot --nginx -d yourdomain.ru -d www.yourdomain.ru

# Получение сертификата для Flask
sudo certbot --nginx -d ai.yourcompany.com
```

### Firewall настройки

#### Российский сервер:
```bash
# Разрешить только необходимые порты
sudo ufw allow 22/tcp    # SSH
sudo ufw allow 80/tcp    # HTTP
sudo ufw allow 443/tcp   # HTTPS
sudo ufw enable
```

#### Зарубежный сервер:
```bash
# Разрешить доступ только с российского сервера
sudo ufw allow from YOUR_RUSSIAN_SERVER_IP to any port 5000
sudo ufw allow 22/tcp    # SSH
sudo ufw allow 80/tcp    # HTTP (для Let's Encrypt)
sudo ufw allow 443/tcp   # HTTPS
sudo ufw enable
```

## 📊 Мониторинг и обслуживание

### Логирование

#### Просмотр логов:
```bash
# Django логи
docker-compose logs -f django

# Flask логи
docker-compose -f docker-compose.flask.yml logs -f flask-ai

# Nginx логи
docker-compose logs -f nginx
```

### Бэкапы

#### База данных:
```bash
# Создание бэкапа
docker-compose exec db pg_dump -U ghostwriter ghostwriter > backup_$(date +%Y%m%d_%H%M%S).sql

# Восстановление
docker-compose exec -T db psql -U ghostwriter ghostwriter < backup.sql
```

#### Медиа файлы:
```bash
# Бэкап медиа файлов
tar -czf media_backup_$(date +%Y%m%d_%H%M%S).tar.gz /opt/ghostwriter/media/

# Автоматический бэкап (cron)
0 2 * * * /path/to/backup-script.sh
```

### Обновления

#### Обновление Django приложения:
```bash
# Получение новой версии
git pull origin main

# Пересборка и перезапуск
docker-compose -f docker-compose.production.yml build django
docker-compose -f docker-compose.production.yml up -d django

# Применение миграций
docker-compose -f docker-compose.production.yml exec django python manage.py migrate
```

#### Обновление Flask микросервиса:
```bash
# На зарубежном сервере
git pull origin main
docker-compose -f docker-compose.flask.yml build flask-ai
docker-compose -f docker-compose.flask.yml up -d flask-ai
```

## ⚖️ Соблюдение 152-ФЗ

### Принципы архитектуры
1. **Локализация персональных данных**: Все ПД хранятся только на российском сервере
2. **Обезличивание для AI**: Flask сервис получает только обезличенные промпты
3. **Шифрование передачи**: Все данные между сервисами шифруются
4. **Аудит доступа**: Логирование всех операций с персональными данными
5. **Территориальное разделение**: Четкое разграничение серверов по юрисдикции

### Документооборот
- Ведите журнал обработки персональных данных
- Документируйте все изменения в архитектуре
- Проводите регулярные аудиты безопасности
- Обеспечьте возможность удаления данных по запросу

## 🎯 Рекомендации по продакшену

### Производительность
- Используйте CDN для статических файлов
- Настройте кеширование Redis
- Оптимизируйте запросы к базе данных
- Мониторьте использование ресурсов

### Безопасность
- Регулярно обновляйте зависимости
- Используйте сильные пароли и ключи
- Настройте автоматические бэкапы
- Мониторьте логи на предмет аномалий

### Масштабирование
- Добавляйте инстансы Flask для увеличения пропускной способности AI
- Используйте load balancer для распределения нагрузки
- Настройте автоматическое масштабирование в облаке
- Мониторьте метрики производительности

---

<div align="center">

**🎉 Готово к продакшену с полным соблюдением российского законодательства! 🇷🇺**

*Архитектура обеспечивает безопасность персональных данных и эффективную AI обработку*

</div>
